#오늘 배운 거
- SHELL, VIM COMMAND
 - ls, cd, mkdir
 - touch, rm, cat# Branch list 
 (local) $ git branch 
 (remote) $ git branch -r 
 (all) $ git branch -a 
 # Create new branch 
 $ git branch {branch name} 
 # switch to branch 
 $ git switch {branch name

 # Delete branch 
 $ git branch -D stem 
 # Push commits on specific branch 
 # 첫 push에는 remote와 local의 branch 연결(-u)이 필요 (이후 수행 필요 x) 
 (first) $ git push -u origin stem 
 $ git push -u origin stem 
 # See the differences 
 $ git diff main stem
 - mark down(가독성 및 설계를 위한 것)

- git 
 -gitigore(이 github에서 가져오지 못하는 것들을 표시/ 설정
 - add, commit, push
 - add(문서에 추가) commit(local 에추가/아직 github에는 없음). push/remote(github로 넘겨서 저장
 - confit(초반 설정/ git config --global user.name “{username}” 내 컴푸터 이름
$ git config --global user.email “{emailaddr}” 내 이메일
$ git config --global core.editor “vim” #vim을 에디터로 사용한다 
$ git config --global core.pager “cat” 읽는 법

- how to star(github의 기본)
$ git clone {username/repo-addr}
$ cd {repo-addr}
$ vi README.md
$ git status
$ git add README.md
$ git commit
$ git push origin main


- pre- commit(파이썬의 패키지 느낌/ commit을 더 쉽게 혹은 더 디테일하게
$ pip install pre-commit (or $ pip3 install pre-commit) 
$ pre-commit --version 
$ touch .pre-commit-config.yaml 
# after setting configuration(or $ pre-commit sample-config > .pre-commit-config.yaml) 
$ pre-commit install 
$ pre-commit run --all-files

 - branch(가지)
 붅기점을 만들어서 새로운 시도하게(게임으로 치면 따로 저장한 데이터로 이동해서 다르게 행동)
# Branch list 
(local) $ git branch 
(remote) $ git branch -r 
(all) $ git branch -a 
# Create new branch 
$ git branch {branch name} 
# switch to branch 
$ git switch {branch name

# Delete branch
$ git branch -D stem
# Push commits on specific branch
# 첫 push에는 remote와 local의 branch 연결(-u)이 필요 (이후 수행 필요 x)
(first) $ git push -u origin stem
$ git push -u origin stem
# See the differences
$ git diff main stem














